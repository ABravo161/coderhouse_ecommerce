spring:
  application:
    name: ecommerce-config
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: pro

management:
  endpoints:
    web:
      exposure:
        include: refresh